# Tool Packages

Portable tool packages live here. Each package owns its own `manifest.json` and `runner.py`.

- `echo_summary`: lightweight echo/summarize demo tool
- `hitl_probe`: placeholder tool that returns approval-oriented metadata
