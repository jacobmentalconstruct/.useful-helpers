# 0041 - T6 Removable MCP Entrance Execution Start

Entry type: execution start
Tranche: T6 Removable MCP Entrance
Status: IMPLEMENTING
Date: 2026-08-29

## Operator approval

The operator instructed the builder to declare T6 and then begin. Entry `0040` declared
the T6 Removable MCP Entrance scope. The builder found no contradiction or blocker in
that declaration and now enters IMPLEMENTING under the same operator direction.

## Implementation boundary

Implementation is limited to the `0040` declaration: a removable MCP stdio adapter over
the existing host/control-plane/state owners, focused product tests, a T6 gate, and
minimal review documentation.

T6 may not park, grant P6 credit, begin T7, widen the mutation surface, add new
mechanical tools, implement release/update/removal lifecycle, or introduce GUI, local AI,
embeddings, domain cartridges, workflow engine, planner, rollback system, remote/cloud
service, or construction-role runtime concepts.

## Next action

Add failing focused T6 fixtures and the T6 gate first, then implement the minimal adapter
against those witnesses. Submit T6 at AWAITING_APPROVAL when verified.
