# T1 Mechanical Hands + Governed Host Execution Start

- Date: 2026-08-26
- Tranche: T1 Mechanical Hands + Governed Host
- Entry class: execution start
- Transition: SCOPE APPROVED / EXECUTE NOT STARTED -> IMPLEMENTING
- Approved declaration: `0008-t1-mechanical-host-declaration.md`
- Scope approval: `0009-t1-declaration-scope-park.md`

## Operator direction

The operator directed the builder to execute the current tranche under the full approved
BCC protocol and stop at AWAITING_APPROVAL. This entry begins implementation under the
scope, non-goals, evidence plan, risks, and ordered plan approved in entries `0008` and
`0009`.

## Remeasured starting state

The repository was clean on `main`. Canonical pytest passed all 10 provisional tests and
Ruff passed before T1 changes. The five tools still import the broad
`core.tool_runtime.ToolContext`; the host still transports `instance_root`,
`target_root`, `state_root`, and `instance_uuid`; no tool body uses UUID or state.

The measured source therefore still matches the declaration's starting premise. No T2
receipt, App Journal, epistemic substrate, awareness, MCP, release, or additional-tool
work enters T1.

## Execution evidence order

1. Add known-answer direct mechanical subprocess fixtures and pre-dispatch refusal
   witnesses.
2. Observe the identity-free fixtures fail against the provisional broad context.
3. Introduce the smallest product-neutral mechanical context and host transport.
4. Adapt the five tools and strengthen their manifest-owned output contracts where the
   fixtures demonstrate need.
5. Consolidate and run the authoritative T1 gate, canonical pytest/Ruff, consumer CLI,
   clean-install/re-entry, dependency, failure-injection, and discovery checks.
6. Submit one immutable AWAITING_APPROVAL entry and stop without parking T1 or starting
   T2.

