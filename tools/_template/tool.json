{
  "_comment": "EXEMPLAR manifest (STATUS: SCAFFOLD, T2). Copy into tools/<id>/tool.json and fill in. Schema: config/README.md.",
  "id": "example",
  "summary": "One-line description of what this tool does.",
  "category": "introspection",
  "authority": "Observe",
  "operates_on": "project",
  "invocation": {
    "interpreter": "${ROOT_VENV_PYTHON}",
    "entry": "tools/example/cli.py"
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "input": {
        "type": "string"
      }
    },
    "required": [
      "input"
    ]
  },
  "output_shape": {
    "type": "object",
    "properties": {
      "ok": {
        "type": "boolean"
      },
      "result": {
        "type": "object"
      }
    }
  }
}
